---
import Layout from "../../layouts/Layout.astro";
import Header from "../../components/layout/Header.astro";
import Footer from "../../components/layout/Footer.astro";
import video from "../../assets/video/video_fondo_success_v2_18022026.mp4";
---

<Layout title="Pedido completado">
  <Header />
  <section class="success">
    <video class="success__video" autoplay muted loop playsinline>
      <source src={video} type="video/mp4" />
    </video>
    <main class="success-page">
      <section class="success-card">
        <p class="success-kicker">Pedido completado</p>
        <h1 class="success-title">Grazas pola túa compra</h1>
        <h3 class="success__h3">Os viquingos xa van de camiño</h3>

        <p class="success-text">
          En breve recibirás un correo de confirmación co resumo do pedido e os
          datos de envío.
        </p>
        <p class="success-text">
          Cando o libro saia do almacén, enviarémosche unha actualización co
          número de seguimento.
        </p>

        <div class="success-actions">
          <a href="/" class="success-btn success-btn--primary"
            >Volver ao inicio</a
          >
          <a href="/contacto" class="success-btn success-btn--ghost"
            >Necesitas axuda?</a
          >
        </div>

        <p class="success-note">
          Se tes algunha dúbida sobre o teu pedido podes escribirnos a
          <span class="success-highlight">info@rubenterre.com</span>.
        </p>
      </section>
    </main>
  </section>
  <Footer />
</Layout>

<style>
  .success {
    position: relative;
    min-height: 100vh;
    width: 100%;
    overflow: hidden;
    background: linear-gradient(
      135deg,
      rgba(11, 9, 7, 1) 15%,
      rgba(11, 9, 7, 0) 100%
    );
  }

  .success::before {
  content: "";
  position: absolute;
  inset: 0;
  background: radial-gradient(circle at top, rgba(0,0,0,0.55), rgba(0,0,0,0.9));
  z-index: -0.5;
}

  /* Vídeo como fondo responsive */
  .success__video {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: -1;
  }
  .success-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 4rem 1.5rem 3rem;
/*     background: radial-gradient(circle at top, #050814 0, #020308 55%);
 */    color: var(--text-color-texto-principal);
 z-index: 50;
 position: relative;
  }

  .success-card {
    width: 100%;
    max-width: 640px;
/*     background: linear-gradient(
      135deg,
      rgba(15, 23, 42, 0.9),
      rgba(15, 23, 42, 0.7)
    ); */
    background-color: var(--theme-color-fondo-principal);
    border-radius: 1.25rem;
    padding: 2.5rem 2.25rem;
    box-shadow: 0 24px 60px rgba(0, 0, 0, 0.7);
    border: 1px solid rgba(148, 163, 184, 0.35);
  }

  .success-kicker {
    text-transform: uppercase;
    letter-spacing: 0.18em;
    font-size: 0.75rem;
    color: var(--gold, #fbbf24);
    margin-bottom: 0.75rem;
  }

  .success-title {
    font-size: clamp(1.9rem, 2.3vw, 2.4rem);
    letter-spacing: -0.03em;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    font-family: var(--font-secondary);
  }

  .success__h3{
    font-size: clamp(1.3rem, 2vw, 2rem);
    line-height: 1.1;
    margin-bottom: 1.5rem;
    font-family: var(--font-secondary);
  }

  .success-text {
    font-size: 0.95rem;
    line-height: 1.7;
    color: var(--text-color-secundario, #cbd5f5);
    margin-bottom: 0.75rem;
    font-family: var(--font-primary);
  }

  .success-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin: 1.75rem 0 1.25rem;
  }

  .success-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.8rem 1.8rem;
    border-radius: 999px;
    font-size: 0.85rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    text-decoration: none;
    font-family: var(--font-primary);
    border: 1px solid transparent;
    cursor: pointer;
    transition:
      transform 0.15s ease,
      box-shadow 0.15s ease,
      filter 0.15s ease,
      background 0.15s ease,
      color 0.15s ease;
  }

  .success-btn--primary {
    background: linear-gradient(135deg, #b45309, #f97316);
    color: #fefce8;
    box-shadow: 0 14px 40px rgba(0, 0, 0, 0.7);
  }

  .success-btn--primary:hover {
    transform: translateY(-1px);
    filter: brightness(1.05);
    box-shadow: 0 18px 50px rgba(0, 0, 0, 0.9);
  }

  .success-btn--ghost {
    background: transparent;
    color: var(--text-color-secundario, #cbd5f5);
    border-color: rgba(148, 163, 184, 0.6);
  }

  .success-btn--ghost:hover {
    background: rgba(15, 23, 42, 0.8);
    color: var(--gold, #fbbf24);
  }

  .success-note {
    font-size: 0.8rem;
    color: #9ca3af;
    font-family: var(--font-primary);
  }

  .success-highlight {
    color: var(--gold, #fbbf24);
    font-weight: 500;
  }

  /* Responsive */
  @media (max-width: 640px) {
      .success-page {
    min-height: auto;
    padding: 3rem 1.5rem 2.5rem;
  }
    .success-card {
      padding: 2rem 1.5rem;
    }

    .success-actions {
      flex-direction: column;
      align-items: stretch;
    }

    .success-btn {
      width: 100%;
      justify-content: center;
    }
  }
</style>
